{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -qE '\\.py$' && /Users/brunofmac/Library/Python/3.9/bin/ruff format \"$f\"; } 2>/dev/null || true",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; git add -A && git diff --staged --quiet || (git commit -m \"auto: $(basename $f)\" && git push); } 2>/dev/null || true",
            "timeout": 30,
            "async": true
          }
        ]
      }
    ]
  }
}
